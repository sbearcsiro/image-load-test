package ala

import io.gatling.core.Predef._
import io.gatling.http.Predef._

import ala._

//class AllImagesSimulation extends Simulation {
//
//  setUp(
//    scenario("all images").exec(apiScenario).exec(storeScenario).inject(
//      users
//    )
//  ).protocols(httpProtocol)
//}
